Create a minting page starting from the pre-existing Telegram/TON Mini App codebase in the zip archive, which already includes TON wallet connection and the base structure.

Objective:
Add a "Minting" section/page that displays available NFTs ready to be minted, fetching metadata dynamically from the specified external index (`nftsMetadataIndex`), and filtering only those that meet the strict criteria (such as valid release dates and remaining max supply limits).

Technical Requirements & Configuration:

1. Configuration File (`src/config.ts` or equivalent):
Create or update a centralized configuration file containing strictly these variables:
- "collectionAddress": "kQDeZQGgQXsn6UK785TT9LVe2MTLZGHEx5jLqE8oOrKMEBHA"
- "collectionMetadata": "https://astounding-flan-ffc457.netlify.app/collectionMetadata.json"
- "nftsMetadataIndex": "https://astounding-flan-ffc457.netlify.app/metadataIndex.json"
- "tonChain": "Testnet" (must support values between Testnet and Mainnet).

2. Minting Criteria & Logic:
- The application must fetch the metadata index from `nftsMetadataIndex`, which lists all individual NFT JSON metadata files.
- **Max Supply & Copies Check**: The web app must verify and enforce that it is impossible to mint more copies than the maximum limit (`maxSupply`) indicated in each NFT's individual JSON metadata file.
- **Price in GRAM**: Minting an NFT requires paying the specific price in GRAM defined in the `mintingPrice` property.
- **Off-Chain Metadata**: The minted NFTs must not store their metadata directly on the blockchain; instead, they must point to and use the metadata hosted at their respective JSON file addresses published on Netlify.
- **Date Restrictions**: The current date/time must fall within the allowed minting window specified for the item/collection.
- The minting process must interact with the collection smart contract (`collectionAddress`) using the connected TON wallet.

3. Reference to Deployment Code & Ed25519 Signature:
- Please inspect the existing codebase/folder containing the **collection deployment code** (name folder: Deploy-Collection-Miniapp) to fully understand the smart contract architecture, item deployment structure, and how off-chain authorizations are handled.
- The backend/API or mint authorization logic must correctly sign or validate transactions using the Ed25519 private key configured via the secret environment variable `AUTHENTICATION_SIGNATURE_PRIVATE_KEY`, aligning with the security scheme implemented in the deployment scripts.

4. UI Design & Vercel Ready:
- **Minimalist Design**: The minting page must remain extremely minimalist and clear, featuring as little text as possible and **no icons or images**.
- Ensure the code is completely clean, properly structured, and ready to be pushed to a GitHub repository and deployed directly to Vercel.
- Properly handle server-side/backend API environment variables for the Ed25519 signature, ensuring `AUTHENTICATION_SIGNATURE_PRIVATE_KEY` is safely read from `process.env`.

Write the complete code, updating the necessary components without losing the existing TON wallet connection functionality. 