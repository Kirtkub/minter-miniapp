{
  "name": "ton-wallet-minting-mini-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node scripts/build-web.mjs",
    "dev": "node scripts/dev-server.mjs"
  },
  "dependencies": {
    "@ton/core": "^0.63.1",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
